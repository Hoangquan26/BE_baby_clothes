import { NotfoundFilterFilter } from './notfound.filter.filter';

describe('NotfoundFilterFilter', () => {
  it('should be defined', () => {
    expect(new NotfoundFilterFilter()).toBeDefined();
  });
});
